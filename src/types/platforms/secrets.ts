export interface SystemSecrets {
  githubToken: string;
  airtableToken: string;
  platformToken: string;
  platformSiteMeta: {
    siteId: string;
    accountId: string;
  };
  webflowTokens: string;
}
